# 打印管理系统

一个基于 Vue 3 + TypeScript 的前端打印管理系统，支持 PC 端和移动端响应式适配。

## 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **TypeScript** - 类型安全的 JavaScript 超集
- **Vite** - 下一代前端构建工具
- **Element Plus** - Vue 3 UI 组件库
- **Pinia** - Vue 状态管理
- **Vue Router** - Vue 官方路由
- **Axios** - HTTP 请求库
- **ECharts** - 可视化图表库
- **Sass** - CSS 预处理器
- **print-js** - 打印工具库

## 功能特性

- 🔐 用户登录/注册
- 🏠 数据概览首页（ECharts 图表）
- 🖨️ 打印中心（上传 Word/PDF、打印机管理、打印设置）
- 📋 数据列表（搜索、筛选、分页、CRUD）
- 📱 响应式设计（PC/平板/手机）
- 🎨 科技感紫色主题
- 🌙 玻璃拟态 UI 风格

## 项目结构

```
src/
├── api/                # API 接口
├── assets/             # 静态资源
│   └── styles/         # 全局样式
├── components/         # 公共组件
│   ├── charts/         # 图表组件
│   ├── common/         # 通用组件
│   └── layout/         # 布局组件
├── hooks/              # 自定义 Hooks
├── router/             # 路由配置
├── stores/             # Pinia 状态管理
├── types/              # TypeScript 类型定义
├── utils/              # 工具函数
└── views/              # 页面视图
    ├── auth/           # 登录/注册
    ├── home/           # 首页
    ├── list/           # 列表页
    └── print/          # 打印页
```

## 快速开始

### 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 后端接口

项目已预留后端接口位置，API 文件中包含模拟数据，对接时替换为实际接口即可。

### 接口列表

| 模块 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 认证 | /api/auth/login | POST | 用户登录 |
| 认证 | /api/auth/register | POST | 用户注册 |
| 认证 | /api/auth/send-code | POST | 发送验证码 |
| 认证 | /api/auth/logout | POST | 用户登出 |
| 首页 | /api/dashboard/stats | GET | 获取统计数据 |
| 打印 | /api/print/upload | POST | 上传文件 |
| 打印 | /api/print/printers | GET | 获取打印机列表 |
| 打印 | /api/print/execute | POST | 执行打印 |
| 打印 | /api/print/history | GET | 打印历史 |
| 列表 | /api/list/data | GET | 获取列表数据 |
| 列表 | /api/list/create | POST | 创建数据 |
| 列表 | /api/list/update/:id | PUT | 更新数据 |
| 列表 | /api/list/delete/:id | DELETE | 删除数据 |

## 配置说明

### 环境变量

创建 `.env.local` 文件配置环境变量：

```env
# API 基础地址
VITE_API_BASE_URL=http://localhost:8080/api
```

### 代理配置

开发环境代理配置在 `vite.config.ts` 中：

```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8080',
    changeOrigin: true
  }
}
```

## 响应式断点

| 设备 | 断点 |
|------|------|
| 移动端 | < 768px |
| 平板 | 768px - 1024px |
| 桌面 | > 1024px |

## 主题色

| 名称 | 色值 | 用途 |
|------|------|------|
| 主色 | #8B5CF6 | 按钮、链接 |
| 主色深 | #7C3AED | Hover 状态 |
| 主色浅 | #A78BFA | 辅助背景 |
| 背景色 | #0F0F23 | 页面背景 |
| 文字色 | #E2E8F0 | 主要文字 |

## 许可证

MIT
